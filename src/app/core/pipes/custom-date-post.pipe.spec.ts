import { CustomDatePostPipe } from './custom-date-post.pipe';

describe('CustomDatePostPipe', () => {
  it('create an instance', () => {
    const pipe = new CustomDatePostPipe();
    expect(pipe).toBeTruthy();
  });
});
